# Cardio Slot redesign — approved implementation contract

Source: user-approved plan, 17 September 2026. Implement in `.worktrees/feature/cardio-slot-redesign` from remote main. All implementation and review remains in the main agent session.

## Direction
Cream enamel cabinet, red CARDIO SLOT marquee, substantial chrome perimeter, dark recessed mechanical reels, red-ball lever with bracket/pivot/rod, integrated deck, printer well/slit/lip. Track, Neon and Mono share geometry. Portrait reference: `/Users/ytchou/.codex/generated_images/01a0ab28-c7b8-77b1-8f26-480cebf76cd1/exec-28db3bd9-e005-4e9c-9936-5eb3dc92bc6e.png`; landscape reference: sibling `exec-27dbf591-7768-4078-9881-ac5f12015c97.png`. References guide appearance, not production assets.

## Milestones and acceptance
1. Domain/compatibility: request snapshot with duration and enabled bookends; exact duration including N−1 Easy 1% 60-second boundary recoveries; minimum 120-second main blocks in existing ranges; deterministic 30-second allocation; MAX 30–60 seconds, 1–2%, ≤10%, followed by equal Easy within same block; independently constructive fallback; final validation; canonical enabled timeline; generation version and request identity. Persist v2 preferences, preview, active run and result with its own plan, on durable changes only. Import legacy active/results with original timing, discard previews, reject malformed data.
2. Machine: integrated portrait and short-landscape cabinet, 48px controls, actual scrolling registered reel strips, accessible lever, one guarded generation per pull and request-ID guarded stages. Storyboard: lever 0–220ms, return/acceleration to 520ms; reels settle 1420/1820/2220ms; hold to 2470ms; paper feed to 3070ms; modal handoff to 3570ms. Motion preference and system reduced motion.
3. Receipt: same leading layout for clipped printer paper and top-layer dialog; viewport measured handoff; fixed heading, independently scrolling every interval, horizontal landscape footer; Close/reopen, Adjust focus, Pull again, Start; scroll lock/restoration and modal focus; resize settles handoff.
4. Run/results: persisted start now+5s, countdown outside session; timestamp-derived reload/visibility/delayed ticks; semantic phases, overall progress, stable effort colors, next-only final-five emphasis, final FINISH; landscape current/next columns with End visible; sheen only ongoing motion; natural completion timestamp wins ending race; actual elapsed overlap; main blocks planned; preserve Canvas 1080×1350 sharing, wake lock, installation/offline/deferred updates.
5. Verification/review/PR: 8,000 plans (500 seeds × 4 durations × 4 bookend combinations), independent constraints/recipes/finish/fallback regression; reducer, persistence and runtime cases; browser journeys including rotation in every stage, receipt focus, sharing, offline and deferred updates; base `/cardio-slot/`; Chromium/WebKit CI. Sequential lint, typecheck, test, base-path build, E2E; visual screenshots at 360×800,390×844,430×932,1440×900,844×390,932×430,844×320; timed mechanical frames. Then review-and-fix and create-pr.

## Constraints and risks
Preserve framework/dependencies/package manager and deployment. No backend/audio/accounts/history/integrations/upgrades. Rotation must not regenerate/restart. Main risk: physical visual cues during exercise cannot be established by emulation. Physical iPhone/Android browser and installed checks plus a real 15-minute treadmill session require human device access and remain explicitly unverified until performed. A failed persistence write must be visible and must not silently permit an update to discard the session. Verification must distinguish emulated evidence from physical acceptance.

## Acceptance update — 17 September 2026
The user explicitly accepted automated verification for this PR and deferred physical iPhone/Android checks and the real treadmill session. Keep those checks documented as follow-up limitations, not a gate for PR readiness.
