# Ticket layout comparison implementation plan

1. Canonicalize new and restored recovery labels to “Recovery” without changing workout timing or persistence shape; cover generation and migration.
2. Add the shared effort guide, static recovery/bookend treatments, and three private main-block interval renderers inside the existing ticket flow.
3. Update responsive styles and the browser journey so exact interval identity, accordion behavior, keyboard focus, and all three options remain verified.
4. Run lint, typecheck, unit tests, production build, Mobile Chrome/Safari E2E, and responsive visual inspection before handing the local prototype back for selection.
